import type { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Contact us',
    description: 'Learn more contact us',
};

const ContactPage = () => {
    return <div>ContactPage</div>;
};

export default ContactPage;
